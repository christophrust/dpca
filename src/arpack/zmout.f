      SUBROUTINE ZMOUT(LOUT, M, N, A, LDA, IDIGIT, IFMT)
      INTEGER            M, N, IDIGIT, LDA, LOUT
      Complex*16         A( LDA, * )
      CHARACTER          IFMT*( * )
      RETURN
      END
